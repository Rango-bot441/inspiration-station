# OpenAI-compatible API endpoint, without the trailing /v1.
AI_API_BASE_URL=https://your-openai-compatible-endpoint.example.com

# Keep real keys in local .env or your deployment platform secret manager.
AI_API_KEY=your-api-key

# Comma-separated fallback models.
AI_MODEL_FALLBACKS=model-a,model-b,model-c
